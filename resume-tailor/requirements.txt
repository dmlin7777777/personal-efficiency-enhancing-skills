# Resume Tailor - Python dependencies

# Core: .docx reading and writing
python-docx>=0.8.11

# PDF reading (priority order — install at least one):
#   1. pdfplumber  — best quality, Python native (recommended)
#   2. PyPDF2      — Python native, no system dependency
#   3. pdftotext   — CLI tool, install separately: https://poppler.freedesktop.org/
pdfplumber>=0.10.0
PyPDF2>=3.0.0

# Optional: ATS checker, diff audit (both built-in, no extra deps)
# Optional: pandoc — for .docx to PDF conversion (install separately)
